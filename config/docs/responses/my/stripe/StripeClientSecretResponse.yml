title: StripeClientSecretResponse
allOf:
  - properties:
      data:
        properties:
          clientSecret:
            type: string
            description: stripeのclient_secret
            example: "seti_dhouahofabgorbaeiurbgu"
        required:
          - clientSecret
required:
  - data